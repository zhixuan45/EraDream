# 剧情引擎开发进度同步手册 (Development Progress)

本文档对比了《剧情引擎框架开发路线图》的原始计划，记录了当前的实现进度，并补充了在开发过程中额外增加的功能。

## 核心架构进度 (Core Architecture)

### 1. 数据层 (Data & Scripting)
- [x] **JSON 中间件**：实现了基于 `System.Text.Json` 的多态序列化系统。
- [x] **多态节点模型**：支持对话、旁白、音乐、选项、分支等多种数据的结构化存储。
- [x] **全局状态管理器**：`GlobalGameState` 已上线，支持运行时变量存储与读取。
- [x] **逻辑判定引擎**：`BranchNode` 已实现基础的变量对比与路径跳转。

### 2. 编辑器系统 (In-Game Editor)
- [x] **可视化图表编辑**：基于 `GraphEdit` 的节点拖拽与连线逻辑。
- [x] **插件化节点架构**：节点定义完全解耦，存放于 `Nodes/` 文件夹，支持无限扩展。
- [x] **详细编辑模式**：每个节点支持 `≡` 菜单展开高级设置，支持 `×` 一键删除。
- [x] **角色/资源管理 GUI**：实现了独立的角色编辑器与资源导入管理界面。
- [x] **项目模式 (Project Mode)**：支持基于文件夹的项目管理，自动扫描 `audio/`, `backgrounds/`, `sprites/` 子目录。
- [x] **开始/结束节点**：已实现专门的剧情入口与出口逻辑，支持自定义返回场景。

### 3. 表现层与播放引擎 (Visuals & Engine)
- [x] **剧情播放引擎**：实现了加载剧本、节点遍历、逻辑跳转的核心循环。
- [x] **立绘/贴纸系统**：**[完成]** 支持多角色同场、位置切换（左/中/右）、表情/差分联动及剪影模式。
- [x] **背景/转场系统**：**[完成]** 支持外部图片实时加载，内置淡入淡出（Fade）转场。
- [x] **打字机效果**：文本支持平滑的逐字显示及点击跳过动画。
- [x] **实时预览系统**：**[完成]** 支持在编辑器内一键调起预览窗口，自动同步节点连线与坐标。

### 4. 多平台与自适应 (Responsive Strategy)
- [x] **全局响应式管理器**：`ResponsiveManager` 实时处理横竖屏切换逻辑。
- [x] **窗口自适应策略**：配置了 `canvas_items` 与 `expand` 模式，解决分辨率适配痛点。
- [x] **跨平台路径适配**：统一使用 `user://` 及原生对话框处理文件读写。

---

## 路线图外新增功能 (Roadmap Extras)
我们在开发过程中，为了提升生产力与健壮性，额外增加了以下功能：

1. **高精度坐标存档系统**：
   - 自动保存所有节点的编辑器位置。
   - 坐标值采用 `Math.Round` 强制保留 **2 位小数**，大幅减小 JSON 剧本文件体积。

2. **外部资源实时渲染引擎**：
   - 绕过了 Godot 默认的 `.import` 限制。
   - 实现了基于 `Image.LoadFromFile` 的物理路径动态加载技术，支持用户随时往项目文件夹丢图。

3. **剧情逻辑自动同步器**：
   - 在预览剧情前，系统会自动扫描 GraphEdit 连线并重构内存中的 `NextNodeId` 树。
   - 确保“所见即所得”，无需手动点击保存即可预览最新逻辑。

4. **防歧义编码标准**：
   - 建立了 `CODING_STANDARDS.md` 规范。
   - 解决了 `Godot.FileAccess` 与 `System.IO.FileAccess` 等 C# 命名空间冲突问题。

5. **自动层级排序算法**：
   - 播放引擎内置自动排序，确保背景始终在底层，UI 始终在顶层，立绘在中间，彻底解决遮挡问题。

6. **全局错误提示与通知系统 (Error & Toast System)**：
   - **[完成]** 实现了 `ErrorNotifier` 全局单例，支持模态对话框（致命错误）与吐司提示（非致命警告）。
   - 适配 Android 及无控制台环境，确保所有平台下用户都能获得直观的错误反馈。
   - 吐司提示支持队列显示，自动处理多条消息的排队与淡入淡出动画。

7. **全局配置与设置菜单 (Settings & Theme System)**：
   - **[完成]** 引入 `SettingsManager` 实现配置持久化，数据存储于 `user://settings.json`。
   - **[完成]** 实现了基于 `CanvasLayer` 的全局设置覆盖层 (`SettingsOverlay`)，支持在任何场景呼出。
   - 内置**深色/浅色模式切换**逻辑，并与加载界面 (`LoadingScreen`) 深度联动。

---

## 待办事项 (Next Steps)
1. **编辑器增强**：增加撤销/重做（Undo/Redo）功能。
2. **AI 剧本转换**：实现文章一键转剧本节点的 AI 工具。
3. **音频系统增强**：支持多通道音轨（BGM, SE, Voice）独立音量控制。
4. [x] **性能优化**：针对大型剧本的加载性能优化。
5. [x] **导出项目功能**：支持将整个剧情项目一键压缩为 zip 文件。
6. [x] **独立预览与可视化编辑**：预览变为不阻塞的独立原生窗口。
