# 剧情引擎开发进度同步手册 (Development Progress)

本文档对比了《剧情引擎框架开发路线图》的原始计划，记录了当前的实现进度，并补充了在开发过程中额外增加的功能。

## 核心架构进度 (Core Architecture)

### 1. 数据层 (Data & Scripting)
- [x] **JSON 中间件**：实现了基于 `System.Text.Json` 的多态序列化系统。
- [x] **多态节点模型**：支持对话、旁白、音乐、选项、分支等多种数据的结构化存储。
- [x] **全局状态管理器**：`GlobalGameState` 已上线，支持运行时变量存储与读取。
- [x] **逻辑判定引擎**：`BranchNode` 已实现基础的变量对比与路径跳转。

### 2. 编辑器系统 (In-Game Editor) - **[重点完成]**
- [x] **可视化图表编辑**：基于 `GraphEdit` 的节点拖拽与连线逻辑。
- [x] **插件化节点架构**：节点定义完全解耦，存放于 `Nodes/` 文件夹，支持无限扩展。
- [x] **详细编辑模式**：每个节点支持 `≡` 菜单展开高级设置，支持 `×` 一键删除。
- [x] **角色选取系统**：预设了结构化的角色选择器，而非简单的文本输入。
- [x] **原生文件系统接入**：通过 `FileIOManager` 呼起 Windows/系统原生保存与打开对话框。
**重要** 节点需要添加开始结束部分


### 3. 表现层与播放引擎 (Visuals & Engine)
- [x] **剧情播放引擎**：实现了加载剧本、节点遍历、逻辑跳转的核心循环。
- [x] **打字机效果**：文本支持平滑的逐字显示及点击跳过动画。
- [x] **音频集成**：支持对话语音绑定及背景音乐节点，包含编辑器内预览播放。
- [ ] **立绘/贴纸系统**：计划中的动态合成系统尚未开始。立绘显示也尚未开始
- [ ] **高级转场特效**：Tween 动画目前仅用于文字，场景转场待开发。

### 4. 多平台与自适应 (Responsive Strategy) - **[重点完成]**
- [x] **全局响应式管理器**：`ResponsiveManager` 实时处理横竖屏切换逻辑。
- [x] **窗口自适应策略**：配置了 `canvas_items` 与 `expand` 模式，解决分辨率适配痛点。
- [x] **跨平台路径适配**：统一使用 `user://` 及原生对话框处理文件读写。

---

## 路线图外新增功能 (Roadmap Extras)
我们在执行过程中根据实际体验，额外增加了以下功能：

1. **动态特效加载界面**：
   - 实现了 27.5 度旋转的密铺弹幕背景。
   - 支持根据屏幕高度动态计算字号和布局。
   - 内置了“深色/浅色模式”切换逻辑及“防闪光弹”灰色滤镜。

2. **剧情浏览器 (Story Selector)**：
   - 原计划直接进入，现增加了专门的剧本选择场景。
   - 支持自动扫描磁盘剧本、实时关键词搜索与过滤。

3. **开发者规范文档**：
   - 额外编写了 `ResponsiveUI_Guide.md`（自适应指南）。
   - 额外编写了 `Node_Specification.md`（节点开发规范）。

---

## 待办事项 (Next Steps)
1. **立绘系统**：开发 `CharacterSprite` 节点，实现多层贴纸合成。
2. **编辑器增强**：增加撤销/重做（Undo/Redo）功能。文章转剧本（AI）功能尚未实现
3. **UI 美化**：为播放器对话框添加更精美的主题皮肤（Theme）。
4. **剧本分包**：处理大型剧本的多文件相互跳转逻辑。
5. **角色列表管理**：对角色列表管理添加GUI支持，同时支持绑定至图像立绘，尚未实现。
6. **资源列表管理**：对图像/音频的管理未实现
